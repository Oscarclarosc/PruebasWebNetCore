@model IEnumerable<PruebasWebNetCore.Web.Data.Entities.AbastecimientoMateriaPrima>

@{
    ViewData["Title"] = "AbastecimientoDeMateriaProcesado";
}

<h1>Abastecimiento De Materia Procesado</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoPedido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoPedido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                <a asp-action="CambiarEstadoAbastecimiento" asp-route-id="@item.Id"> Confirmar Entrega</a>
            </td>
        </tr>
}
    </tbody>
</table>
